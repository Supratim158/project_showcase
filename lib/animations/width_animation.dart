import 'package:flutter/material.dart';

class WidthAnimation extends StatelessWidget {
  const WidthAnimation({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
